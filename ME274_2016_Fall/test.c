#include "stdio.h"
#include "stdlib.h"

int main (int argc, char** argv) {
	int level = 3;
	int offset = 0;
	char data[];
	char[offset, offset+1, offset+2] = [level, level, level];
	return EXIT_SUCCESS;
}
